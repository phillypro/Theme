

// one show index
$(document).on( "pagecreate", '#index', function( event, ui ) {
$(document).one("pagecontainerbeforeshow", function (event,ui) {
  if (ui.toPage[0].id == 'index') {
    var page = ui.toPage[0];
    window.scrollTo(0, 0);

     var slider = jQuery(page).find('.rev_slider');
     if(!slider.hasClass('revslider-initialised')) {
        createhomepageslider();
       
   function logLoaded3() {
  var dataPrev = ui.prevPage[0] ;
  if(typeof dataPrev != 'undefined') {     
     var slider = jQuery(page).find('.rev_slider');
     if(!slider.hasClass('revslider-initialised')) {
       slider.revstart();
     }
  }else{
     setTimeout(function() {
     var slider = jQuery(page).find('.rev_slider');
     if(!slider.hasClass('revslider-initialised')) {
       slider.revstart();
     }
  },2000);
  }
    }

    (function listen3() {
        if (window.loaded) {
            logLoaded3();

        } else {
            window.setTimeout(listen3, 50);
        }
    })();
       
      }else{
      slider.revredraw();
      slider.revresume();
     } 
  }
});
});




// createhomepage slider
function setREVStartSize(e) {
    try {
        e.c = jQuery(e.c);
        var i = jQuery(window).width(),
            t = 9999,
            r = 0,
            n = 0,
            l = 0,
            f = 0,
            s = 0,
            h = 0;
        if (e.responsiveLevels && (jQuery.each(e.responsiveLevels, function(e, f) {
                f > i && (t = r = f, l = e), i > f && f > r && (r = f, n = e)
            }), t > r && (l = n)), f = e.gridheight[l] || e.gridheight[0] || e.gridheight, s = e.gridwidth[l] || e.gridwidth[0] || e.gridwidth, h = i / s, h = h > 1 ? 1 : h, f = Math.round(h * f), "fullscreen" == e.sliderLayout) {
            var u = (e.c.width(), jQuery(window).height());
            if (void 0 != e.fullScreenOffsetContainer) {
                var c = e.fullScreenOffsetContainer.split(",");
                if (c) jQuery.each(c, function(e, i) {
                    u = jQuery(i).length > 0 ? u - jQuery(i).outerHeight(!0) : u
                }), e.fullScreenOffset.split("%").length > 1 && void 0 != e.fullScreenOffset && e.fullScreenOffset.length > 0 ? u -= jQuery(window).height() * parseInt(e.fullScreenOffset, 0) / 100 : void 0 != e.fullScreenOffset && e.fullScreenOffset.length > 0 && (u -= parseInt(e.fullScreenOffset, 0))
            }
            f = u
        } else void 0 != e.minHeight && f < e.minHeight && (f = e.minHeight);
        e.c.closest(".rev_slider_wrapper").css({
            height: f
        })
    } catch (d) {
        console.log("Failure at Presize of Slider:" + d)
    }
};



function createhomepageslider() {
  var htmlDiv = document.getElementById("rs-plugin-settings-inline-css"); var htmlDivCss="";
				if(htmlDiv) {
					htmlDiv.innerHTML = htmlDiv.innerHTML + htmlDivCss;
				}else{
					var htmlDiv = document.createElement("div");
					htmlDiv.innerHTML = "<style>" + htmlDivCss + "</style>";
					document.getElementsByTagName("head")[0].appendChild(htmlDiv.childNodes[0]);
				}
  	var htmlDivCss = '	#rev_slider_140_1_wrapper .tp-loader.spinner4 div { background-color: #ffffff !important; } ';
					var htmlDiv = document.getElementById('rs-plugin-settings-inline-css');
					if(htmlDiv) {
						htmlDiv.innerHTML = htmlDiv.innerHTML + htmlDivCss;
					}
					else{
						var htmlDiv = document.createElement('div');
						htmlDiv.innerHTML = '<style>' + htmlDivCss + '</style>';
						document.getElementsByTagName('head')[0].appendChild(htmlDiv.childNodes[0]);
					}
 		var htmlDivCss = '	#rev_slider_141_1_wrapper .tp-loader.spinner4 div { background-color: #ffffff !important; } ';
					var htmlDiv = document.getElementById('rs-plugin-settings-inline-css');
					if(htmlDiv) {
						htmlDiv.innerHTML = htmlDiv.innerHTML + htmlDivCss;
					}
					else{
						var htmlDiv = document.createElement('div');
						htmlDiv.innerHTML = '<style>' + htmlDivCss + '</style>';
						document.getElementsByTagName('head')[0].appendChild(htmlDiv.childNodes[0]);
					}

if (setREVStartSize!==undefined) setREVStartSize(
	{c: '#rev_slider_140_1', responsiveLevels: [1600,1441,778,601], gridwidth: [1920,1440,778,480], gridheight: [745,500,350,423], sliderLayout: 'auto'});
  
  var revapi141,
        tpj = jQuery;
    if (tpj("#rev_slider_141_1").revolution == undefined) {
        revslider_showDoubleJqueryError("#rev_slider_141_1");
    } else {
        revapi140 = tpj("#rev_slider_141_1").show().revolution({
  			sliderLayout:"auto",
			dottedOverlay:"none",
			delay:9000,
			navigation: {
				onHoverStop:"off",
			},
			responsiveLevels:[1600,1441,778,601],
			visibilityLevels:[1600,1441,778,601],
			gridwidth:[1920,1440,778,480],
			gridheight:[664,500,400,440],
			lazyType:"none",
			scrolleffect: {
				fade:"on",
				on_slidebg:"on",
				disable_on_mobile:"off",
			},
			parallax: {
				type:"scroll",
				origo:"enterpoint",
				speed:400,
				speedbg:10,
				speedls:0,
				levels:[5,10,15,20,25,30,35,40,45,46,47,48,49,50,51,55],
			},
			shadow:0,
			spinner:"spinner4",
			stopLoop:"off",
			stopAfterLoops:-1,
			stopAtSlide:-1,
			shuffle:"off",
			autoHeight:"off",
			disableProgressBar:"on",
			hideThumbsOnMobile:"off",
			hideSliderAtLimit:0,
			hideCaptionAtLimit:0,
			hideAllCaptionAtLilmit:0,
			debugMode:false,
			fallbacks: {
				ignoreHeightChanges:"mobile",
				ignoreHeightChangesSize:0,
				simplifyAll:"off",
				nextSlideOnWindowFocus:"off",
				disableFocusListener:true,
			}
		});
    }
}







